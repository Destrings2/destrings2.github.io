/**
 * What the app calls itself before it knows whose home it is looking at.
 *
 * Deliberately not a place. The bundle is public, and a street name identifies
 * a household more directly than the floorplan that was taken out of it did.
 * Once you are signed in, the household's own name is shown instead.
 */
export const APP_NAME = 'Rota';
export const APP_TAGLINE = 'house & chores';
