# Copy to .env.local and fill in from your Supabase project's API settings.
# Leave these unset and the app runs entirely on-device against IndexedDB,
# which is exactly how it behaved before there was a backend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
